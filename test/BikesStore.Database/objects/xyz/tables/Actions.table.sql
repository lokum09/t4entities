CREATE TABLE [xyz].[Actions] (
    [Id]                            TINYINT         NOT NULL,
    [Name]                          NVARCHAR (100)  NOT NULL,
    CONSTRAINT [PK_xyz.Actions] PRIMARY KEY CLUSTERED ([Id])
);
GO