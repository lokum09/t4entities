CREATE TABLE [xyz].[SystemActions] (
    [Id]                            TINYINT         NOT NULL,
    [Name]                          NVARCHAR (100)  NOT NULL,
    CONSTRAINT [PK_xyz.SystemActions] PRIMARY KEY CLUSTERED ([Id])
);
GO